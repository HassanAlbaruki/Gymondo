# Gymondo
kotlin

fixed (-) Description is not parsed

fixed (-) search List does not remains Paginated

fixed (-) Body parts are hardcoded and not fetched from the API

fixed (-) Search and filter don’t work well together.

using new libraries like lotti & android-networking
